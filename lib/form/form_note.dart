import 'package:flutter/material.dart';

class FormNote extends StatefulWidget {
  @override
  FormNoteState createState() => FormNoteState();
}

class FormNoteState extends State<FormNote> {
  @override
  
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
